@model IEnumerable<ShopBon.Models.Category>
    <p class="lead">Các Loại Sản Phẩm </p>
    <div class="list-group">
        @foreach (var item in Model)
        {
       
            <a href="@Url.Action("SanPhamTheoLoai","Site",new {id = item.Id })" class="list-group-item">
                @item.Name
            </a>
        }
    </div>